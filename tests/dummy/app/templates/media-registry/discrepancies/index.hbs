<div class="master-collection__wrapper">
  <IsolatedCollection
    @class="discrepancies-list"
    @model={{@model}}
    @format="table-list"
    @mode="view"
    @sort={{this.sort}}
    @search={{this.search}}
    @removeItem={{this.removeItem}}
  />
</div>