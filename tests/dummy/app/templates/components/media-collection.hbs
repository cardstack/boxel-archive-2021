<section class="collection">
  <header>
    <h2>{{humanize @model.title}}</h2>
    <div>
      {{!-- bulk select indicator --}}
      {{!-- filters --}}
      {{!-- view selector --}}
    </div>
  </header>

  <ul class="collection-items">
    {{#each @model.collection as |item|}}
      <li>
        {{yield item}}
      </li>
    {{/each}}

    {{!-- Add new --}}
  </ul>
</section>