<div>
  <Card @class="territory-card territory-card--{{format-id @model.title}}" @model={{@model}} @mode="view" />
  <ul class="territory__publishers-list">
    {{#each @model.value as |card|}}
      <li>
        <Card @model={{card}} @mode="view" />
      </li>
    {{/each}}
  </ul>
</div>