{{#if this.registrationEmbedded}}
  <Card
    @mode="view"
    @model={{this.registrationEmbedded}}
    @status={{if @status "no-change"}}
    @statusText={{if @status "Specific to this version"}}
  />
{{else}}
  N/A
{{/if}}