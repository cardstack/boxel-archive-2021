<div role="button" {{action this.trayAction}} class="boxel-tray {{if this.expanded "expanded"}}">
  {{yield}}
<button {{on "click" this.trayAction}} class="boxel-tray {{if this.expanded "expanded"}}">
  {{yield}}
</div>
